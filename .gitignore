# Real credentials — NEVER commit. Baked into the Singularity image at build time only.
SECRETS.json
**/SECRETS.json

# Python
__pycache__/
*.pyc

# Local scratch / outputs
output/
*.sif

# Presigned S3 URLs to the creds-baked .sif — secret, never commit
# Broad match: any name containing "presigned" (e.g. presigned_*.txt, *-presigned.txt).
*presigned*
